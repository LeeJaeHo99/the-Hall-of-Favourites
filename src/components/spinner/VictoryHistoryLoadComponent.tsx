import LoadSpinner from "./LoadSpinner";

export default function VictoryHistoryLoadComponent() {
    return (
        <div className="victory-history victory-history--load blur-box">
            <LoadSpinner />
        </div>
    );
}
